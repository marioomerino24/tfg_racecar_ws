<launch>
  <arg name="cfg" default="$(find racecar_ackermann_mux)/config/mux.yaml"/>

  <!-- Alias de compatibilidad: launch recomendado en core/. -->
  <include file="$(find racecar_ackermann_mux)/launch/core/ackermann_mux.launch">
    <arg name="cfg" value="$(arg cfg)"/>
  </include>
</launch>
