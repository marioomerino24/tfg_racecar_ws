<launch>
  <arg name="use_yaml" default="$(find racecar_pure_pursuit)/config/pure_pursuit.yaml"/>

  <!-- Alias de compatibilidad: launch recomendado en core/. -->
  <include file="$(find racecar_pure_pursuit)/launch/core/target_selector.launch">
    <arg name="config" value="$(arg use_yaml)"/>
  </include>
</launch>
